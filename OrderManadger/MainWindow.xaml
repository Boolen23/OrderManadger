<Window x:Class="OrderManadger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrderManadger"
        xmlns:conv="clr-namespace:OrderManadger.Converters"
        xmlns:vm ="clr-namespace:OrderManadger.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary Source="MainDictionary.xaml"/>   
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <StackPanel>
        <StackPanel>
            <Button Content="Добавить заказ" Command="{Binding AddOrderCommand}" Width="100" HorizontalAlignment="Left"/>
            <ContentPresenter Content="{Binding NewOrderList}" ContentTemplate="{StaticResource OrdersTemplate}"/>
            <TextBox TextWrapping="Wrap" AcceptsReturn="True" SpellCheck.IsEnabled="True"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Base}">
            <ListBox.ItemTemplate>
                <ItemContainerTemplate>
                    <StackPanel Background="{Binding status, Converter={StaticResource StatusToBrush}, Mode=OneWay}">
                        <TextBlock Text="{Binding Orders}"/>
                        <TextBlock Text="{Binding Comment}"/>
                        <TextBlock Text="{Binding Date}"/>
                    </StackPanel>
                </ItemContainerTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>
